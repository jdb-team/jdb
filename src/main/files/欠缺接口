1、好友申请 需要建一个表，
字段 ：
id cur_user_id apply_user_id message state 0 (同意,不同意)
并将对应 FriendController类中接口
String queryUnFriend(PageResult<User> pageResult, String phone, Alumnus alumnus, String orderby);改写
